# توثيق شامل — نظام إدارة مكتب الصيرفة (Sarrafa System)

نظام ويب متكامل لإدارة مكتب صيرفة متخصص ببيع وشراء الدولار الأمريكي مقابل الدينار العراقي، مبني وفق معمارية MVC مع فصل كامل بين Backend وFrontend.

---

## 1) نظرة عامة على المعمارية

```
المتصفح (Frontend: HTML/CSS/JS)
        │  fetch() + كوكيز JWT
        ▼
Express.js Server (Node.js + TypeScript)
        │
        ├── Middlewares (أمان، صلاحيات، تحقق من المدخلات)
        ├── Modules (Controller → Service → Prisma)
        │
        ▼
Prisma ORM
        │
        ▼
Microsoft SQL Server (قاعدة البيانات)
```

**نمط التصميم المتبع لكل وحدة (Module):**

```
route.ts        → يحدد المسار (URL) والصلاحية المطلوبة والـ validation
   │
controller.ts   → يستقبل الطلب، يستدعي service، يُعيد الاستجابة
   │
service.ts      → منطق العمل الفعلي (الحسابات، قواعد العمل، الاتصال بقاعدة البيانات)
   │
validation.ts   → قواعد Zod للتحقق من صحة المدخلات قبل وصولها للـ controller
```

هذا الفصل (Separation of Concerns) يجعل كل طبقة قابلة للاختبار والتعديل بشكل مستقل، وهو معيار معتمد في الأنظمة المالية الاحترافية.

---

## 2) هيكل المشروع الكامل

```
sarrafa-system/
│
├── prisma/
│   ├── schema.prisma          ← تعريف قاعدة البيانات بالكامل (9 جداول)
│   └── seed.ts                 ← بيانات أولية (مستخدم مدير + أدوار)
│
├── src/
│   ├── config/
│   │   ├── env.ts               ← قراءة والتحقق من متغيرات البيئة
│   │   ├── database.ts          ← اتصال Prisma بقاعدة البيانات (Singleton)
│   │   └── logger.ts            ← إعداد Winston للتسجيل
│   │
│   ├── middlewares/
│   │   ├── auth.middleware.ts       ← التحقق من JWT
│   │   ├── role.middleware.ts       ← التحكم بالصلاحيات (RBAC)
│   │   ├── error.middleware.ts      ← معالجة مركزية لكل الأخطاء
│   │   ├── rateLimiter.middleware.ts← الحماية من هجمات Brute Force
│   │   └── validate.middleware.ts   ← تطبيق قواعد Zod
│   │
│   ├── modules/
│   │   ├── auth/            ← تسجيل الدخول، Refresh Token، تغيير كلمة المرور
│   │   ├── users/           ← إدارة المستخدمين (مدير فقط)
│   │   ├── exchangeRates/   ← تحديد وتحديث أسعار الصرف
│   │   ├── transactions/    ← عمليات البيع والشراء (القلب التشغيلي)
│   │   ├── treasury/        ← رصيد الخزينة ولوحة التحكم
│   │   ├── reports/         ← التقارير وتصدير Excel/PDF
│   │   └── auditLogs/       ← سجل تدقيق كل الإجراءات الحساسة
│   │
│   ├── utils/
│   │   ├── ApiError.ts          ← كائن موحّد للأخطاء
│   │   ├── ApiResponse.ts       ← صياغة موحّدة للاستجابات الناجحة
│   │   ├── catchAsync.ts        ← التقاط أخطاء async تلقائيًا
│   │   └── weightedAverage.ts   ← دوال حساب متوسط التكلفة والربح (الأهم)
│   │
│   ├── types/express.d.ts   ← توسيع Express.Request ليشمل بيانات المستخدم المصادَق
│   ├── app.ts                ← تجميع كل الوسائط والمسارات
│   └── server.ts             ← نقطة التشغيل + Graceful Shutdown
│
├── public/                   ← الواجهة الأمامية (تُخدَّم مباشرة من Express)
│   ├── css/style.css          ← تصميم بنكي كامل RTL
│   ├── js/api.js               ← طبقة موحدة للاتصال بالـ API
│   ├── js/layout.js            ← Sidebar + Navbar مشترك بين كل الصفحات
│   ├── index.html              ← صفحة تسجيل الدخول
│   └── pages/
│       ├── dashboard.html       ← لوحة التحكم
│       ├── transactions.html    ← تنفيذ عملية بيع/شراء
│       ├── transaction-log.html ← سجل العمليات (بحث/فلترة/تعديل/حذف)
│       ├── exchange-rate.html   ← إعدادات السعر (مدير)
│       ├── reports.html         ← التقارير + تصدير
│       ├── users.html           ← إدارة المستخدمين (مدير)
│       └── audit-log.html       ← سجل التدقيق (مدير)
│
├── package.json / tsconfig.json / .env / .env.example / .gitignore
└── README.md                  ← دليل التشغيل خطوة بخطوة
```

---

## 3) قاعدة البيانات (9 جداول)

| الجدول | الوظيفة |
|---|---|
| `Role` | الأدوار: ADMIN / EMPLOYEE |
| `User` | المستخدمون، مرتبطون بدور واحد |
| `ExchangeRate` | كل سعر صرف يُحفظ كسجل جديد عند التغيير (لا يُحذف القديم) — لضمان أن العمليات القديمة تبقى مرتبطة بسعرها الأصلي |
| `Customer` | بيانات الزبائن (اختيارية) |
| `Transaction` | كل عملية بيع/شراء، مرتبطة بالموظف والسعر المستخدم وقت التنفيذ |
| `Treasury` | سجل واحد يمثل الرصيد الحالي (دولار + دينار + متوسط التكلفة) |
| `ProfitHistory` | تجميع يومي للأرباح (لتسريع التقارير دون فحص كل العمليات في كل مرة) |
| `AuditLog` | سجل كل إجراء حساس: من، متى، من أي IP، ماذا فعل |
| `Setting` | إعدادات عامة قابلة للتوسيع مستقبلًا |

**علاقات المفاتيح الأجنبية الأساسية:**
- `User.roleId → Role.id`
- `Transaction.employeeId → User.id`
- `Transaction.customerId → Customer.id` (اختياري)
- `Transaction.exchangeRateId → ExchangeRate.id`
- `ExchangeRate.setById → User.id`
- `AuditLog.userId → User.id`

كل الحقول المالية (`Decimal`) استُخدمت بدل `Float` لتفادي مشاكل التقريب الشائعة في الحسابات المالية.

---

## 4) منطق العمل الأهم: حساب الربح بمتوسط التكلفة المرجّح (Weighted Average Cost)

هذا هو الجزء الأكثر حساسية في النظام، وقد طُلب صراحة استخدام هذه الطريقة عند وجود عمليات شراء متعددة بأسعار مختلفة.

### الفكرة

الخزينة تحتفظ بحقل `avgCostPrice` يمثل متوسط السعر الذي اشترى به المكتب كل دولار موجود حاليًا في الخزينة.

### عند كل عملية **شراء** جديدة:

```
المتوسط الجديد = ((الرصيد الحالي × المتوسط الحالي) + (الكمية الجديدة × سعر الشراء الجديد))
                  ÷ (الرصيد الحالي + الكمية الجديدة)
```

**مثال عملي:**
- الخزينة تحتوي حاليًا على 1000$ بمتوسط تكلفة 1390 دينار.
- تمت عملية شراء جديدة: 500$ بسعر 1395 دينار.
- المتوسط الجديد = ((1000×1390) + (500×1395)) ÷ 1500 = **1391.67** دينار.

### عند كل عملية **بيع**:

```
الربح = (سعر البيع - متوسط التكلفة الحالي) × عدد الدولارات المباعة
```

**مثال:** إذا كان متوسط التكلفة 1391.67 وسعر البيع 1415، وبيع 1000$:
الربح = (1415 - 1391.67) × 1000 = **23,330 دينار**

هذه المعادلات مطبَّقة في ملف `src/utils/weightedAverage.ts`، وتُستدعى من `transaction.service.ts` ضمن كل عملية بيع/شراء.

---

## 5) دورة حياة عملية البيع/الشراء (الأهم في النظام)

كل عملية تُنفَّذ داخل **Prisma Transaction واحدة** (`prisma.$transaction`) لضمان أن كل الخطوات التالية تنجح معًا أو تفشل معًا (Atomicity)، وهذا حاسم في نظام مالي:

1. جلب السعر الحالي النشط (BUY/SELL price).
2. جلب رصيد الخزينة الحالي.
3. **إذا كانت شراء:** التحقق من كفاية رصيد الدينار → حساب متوسط التكلفة الجديد → زيادة رصيد الدولار → إنقاص رصيد الدينار.
4. **إذا كانت بيع:** التحقق من كفاية رصيد الدولار (**منع البيع بأكثر من الرصيد المتاح** كما طُلب) → حساب الربح بمتوسط التكلفة → إنقاص رصيد الدولار → زيادة رصيد الدينار.
5. حفظ سجل العملية (`Transaction`) مع كل التفاصيل.
6. تحديث سجل الأرباح اليومي (`ProfitHistory`) عبر `upsert` (إنشاء أو تحديث سجل اليوم).

### عند حذف عملية (مدير فقط):
النظام **يعكس تأثيرها تلقائيًا** على الخزينة وسجل الأرباح اليومي، ويرفض الحذف إذا كان سيؤدي إلى رصيد سالب (أي أن عمليات لاحقة تعتمد على وجود ذلك الرصيد).

---

## 6) الأمان (Security)

| الآلية | التطبيق |
|---|---|
| **JWT** | Access Token (صلاحية 8 ساعات) + Refresh Token (7 أيام)، محفوظان في كوكيز `HttpOnly` (غير قابلين للقراءة من JavaScript، حماية من XSS) |
| **bcrypt** | تشفير كلمات المرور بـ 12 جولة تشفير (Salt Rounds) |
| **RBAC** | `authorize(RoleName.ADMIN)` كوسيط يمنع الموظفين من الوصول لمسارات المدير على مستوى الـ API مباشرة (وليس فقط إخفاء الأزرار في الواجهة) |
| **Rate Limiting** | حد عام 100 طلب/15 دقيقة، وحد صارم 5 محاولات دخول فاشلة/15 دقيقة لمنع Brute Force |
| **Zod Validation** | كل مدخل يُفحص شكليًا (نوع البيانات، الحدود، الصيغة) قبل وصوله لأي منطق عمل |
| **Helmet** | رؤوس HTTP أمنية قياسية (حماية من بعض أنواع XSS/Clickjacking) |
| **CORS** | مقيّد بنطاق `CLIENT_URL` المحدد في `.env` فقط |
| **حماية SQL Injection** | Prisma يبني الاستعلامات بشكل آمن (Parameterized Queries) تلقائيًا؛ لا يوجد أي SQL خام في الكود |
| **Audit Log** | كل تسجيل دخول (ناجح/فاشل)، تعديل سعر، حذف عملية، إنشاء/تعديل مستخدم يُسجَّل مع الوقت وعنوان IP |
| **Soft Delete** | حذف المستخدمين والعمليات لا يكون فعليًا (بل تعطيل/وسم) للحفاظ على سلامة السجل التاريخي المطلوب قانونيًا وماليًا |

---

## 7) قائمة نقاط الوصول (API Endpoints)

### Auth (`/api/auth`)
| Method | المسار | الوصف | الصلاحية |
|---|---|---|---|
| POST | `/login` | تسجيل دخول | عام |
| POST | `/refresh` | تحديث الجلسة | عام |
| POST | `/logout` | تسجيل خروج | مُسجَّل دخول |
| GET | `/me` | بيانات المستخدم الحالي | مُسجَّل دخول |
| PATCH | `/change-password` | تغيير كلمة المرور | مُسجَّل دخول |

### Users (`/api/users`) — مدير فقط
`GET /` · `POST /` · `PATCH /:id` · `DELETE /:id`

### Exchange Rates (`/api/exchange-rates`)
`GET /current` · `GET /history` · `POST /` (مدير فقط)

### Transactions (`/api/transactions`)
`GET /` (بحث/فلترة/ترتيب/صفحات) · `GET /:id` · `POST /` · `PATCH /:id` · `DELETE /:id` (مدير فقط)

### Treasury (`/api/treasury`)
`GET /balance` · `GET /dashboard`

### Reports (`/api/reports`) — مدير فقط
`GET /profit?period=today|week|month|year|all` · `GET /export/excel` · `GET /export/pdf`

### Audit Logs (`/api/audit-logs`) — مدير فقط
`GET /?page=&limit=&action=&userId=`

جميع الاستجابات بصيغة موحدة:
```json
{ "success": true, "message": "...", "data": { ... } }
```
أو عند وجود صفحات:
```json
{ "success": true, "data": [...], "pagination": { "page":1, "limit":20, "total":50, "totalPages":3 } }
```

---

## 8) الواجهة الأمامية (Frontend)

بُنيت بـ **HTML5 + CSS3 + JavaScript صرف** (بدون Framework وبدون خطوة Build) لتعمل مباشرة من مجلد `public` دون أي تجهيز إضافي — هذا قرار تصميمي للتبسيط، ويمكن تحويلها لاحقًا إلى TypeScript مُجمَّع (Webpack/Vite) كمرحلة منفصلة إذا رغبت.

**نمط العمل:**
- `layout.js` يبني الـ Sidebar والـ Navbar ديناميكيًا في كل صفحة، ويُخفي عناصر القائمة المخصصة للمدير تلقائيًا إذا كان المستخدم موظفًا (اعتمادًا على `/api/auth/me`).
- `api.js` طبقة موحدة فوق `fetch()`، تتعامل تلقائيًا مع تجديد JWT عند انتهاء صلاحيته (401 → محاولة `/auth/refresh` تلقائيًا → إعادة الطلب الأصلي).
- كل صفحة HTML مستقلة وتستدعي الـ API مباشرة عبر JavaScript inline، بدون حالة مشتركة معقدة (مناسب لحجم هذا النظام).
- تصميم `style.css` يحاكي الأنظمة البنكية: شريط جانبي داكن، بطاقات إحصائية، جداول بيانات، نوافذ منبثقة (Modals) للتعديل/الإضافة، ودعم كامل للشاشات الصغيرة (Responsive).

---

## 9) القيود المعروفة (يجب معرفتها قبل الإنتاج)

1. **تصدير PDF بالعربية:** مكتبة `pdfkit` لا "تُشكِّل" الحروف العربية (لا تصل الحروف ببعضها بصريًا) دون خط عربي معالَج خصيصًا مع مكتبة تشكيل مثل `arabic-reshaper`. لذلك جدول الـ PDF حاليًا بعناوين إنجليزية لضمان عرض سليم. **تصدير Excel يدعم العربية والـ RTL بالكامل والصحيح.**
2. **حذف عمليات شراء قديمة:** لا يُعاد بناء متوسط التكلفة (Weighted Average) بأثر رجعي بدقة كاملة إذا حُذفت عملية شراء قديمة تلتها عمليات أخرى معتمِدة عليها. القاعدة المعيارية في الأنظمة المالية الحقيقية: **لا تُحذف عمليات الشراء التاريخية إطلاقًا**، بل تُستخدم "قيود تسوية" (Adjustment Entries) بدلًا من الحذف.
3. **لا توجد شاشة "رصيد افتتاحي"** لإدخال رصيد ابتدائي مباشر للخزينة دون المرور بعملية شراء فعلية (موضح الحل البديل في `README.md`).
4. **لا توجد اختبارات آلية (Unit/Integration Tests)** بعد — كانت من ضمن الخطوات المطلوبة أصلًا (رقم 11 في خطتك) ولم تُنفَّذ بعد.
5. لم تُجرَ مراجعة أمنية اختراقية (Penetration Testing) فعلية — الإجراءات المطبقة هي "أفضل الممارسات" القياسية فقط.

---

## 10) خطوات التشغيل السريعة

راجع ملف `README.md` المرفق داخل مجلد المشروع للتفاصيل الكاملة خطوة بخطوة (تثبيت الحزم، إعداد `.env`، تشغيل Migration، الـ Seed، والتشغيل).

ملخص سريع:
```bash
npm install
# عدّل .env بمعلومات SQL Server الخاصة بك
npm run prisma:migrate
npx ts-node prisma/seed.ts
npm run dev
```
بيانات الدخول الأولية: `admin` / `Admin@12345` (**غيّرها فورًا**).

---

## 11) الخطوات التالية المقترحة (لم تُنفَّذ بعد ضمن هذه الجولة)

- كتابة اختبارات Unit/Integration (Jest أو Vitest) لخدمات `transaction.service.ts` تحديدًا، لأنها الأكثر حساسية ماليًا.
- تحسين الأداء: فهارس إضافية على الاستعلامات الشائعة، Caching لسعر الصرف الحالي.
- شاشة رصيد افتتاحي.
- دعم PDF عربي كامل عبر خط مُدمَج ومكتبة تشكيل.
- تدقيق أمني شامل قبل أي استخدام إنتاجي فعلي في بيئة تحوي أموالًا حقيقية.
